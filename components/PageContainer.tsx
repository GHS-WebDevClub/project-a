import styled from "styled-components";

export default styled.div`
  background: #1c1c1e;
  min-height: 100vh;
  padding: 2rem 0;

  p, label, legend {
    color: #cacaca;
  }

  fieldset {
    outline: none;
    border: none;
    width: 100%;
  }
`;